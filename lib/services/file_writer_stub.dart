// Web stub — no dart:io available on Flutter Web.
// writeFileBytes is a no-op on web; the caller uses kIsWeb guard
// so this is never actually invoked.
Future<void> writeFileBytes(String path, List<int> bytes) async {
  // No-op on web
}
